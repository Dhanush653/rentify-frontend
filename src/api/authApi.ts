/**
 * Auth endpoints.
 * TODO: implement using `api` and API_ROUTES.AUTH — e.g.
 *   login: (payload: LoginRequest) =>
 *     api.post<AuthResponse>(API_ROUTES.AUTH.LOGIN, payload).then((res) => res.data)
 */
export const authApi = {}
